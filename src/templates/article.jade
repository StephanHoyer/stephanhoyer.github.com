
extends layout

block prepend title
  #{ page.title } -

block header

block content
  include includes/author
  include includes/tags
  a.back(href='/') 
    i.icon-chevron-left
    | &nbsp;back to index
  h1= page.title
  p
    mixin tags(page)
    mixin author(page.metadata.author)

  article.post
    section!= page.html

  a.back(href='/') 
    i.icon-chevron-left
    | &nbsp;back to index
